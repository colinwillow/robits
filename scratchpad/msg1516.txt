CMB1516 - the two quantum machines are now the same object by construction (chunk 1 of your list)

The customise-screen unit stood flat on the floor; the deploy-room one arrived on a mast. That is
why it reads as a different machine either side of the handoff - it is not just popping, it changes
SHAPE. The mast was built inline in the stage code only, so the two silhouettes were kept in step by
hand, which is to say not at all.

The mast (plinth, glow ring, ribbed column, rib stack) is now one shared builder, _quantumPost, and
both sites call it. The customise unit is lifted by the same ratio the stage uses - the stage raises
its unit to the pilot's foot plane, which measures ~0.18 of the unit's own height - so the two are
identical by construction rather than by matching numbers.

  verified: the stage unit builds its mast through the shared path (61 sibling meshes, torus ring
  present) - unchanged from before, which is the point.

  NOT verified: the customise-screen unit rendering with its new mast. _dressJack only exists once
  you are inside the customise sub-screen and I could not drive the harness there; the code path is
  shared with the stage version, but I have not seen it. That one is genuinely yours to check.

  window._DRESS_JACK_POST=0 puts it back flat on the floor if the mast reads wrong there.

STILL ON THE LIST from your five screenshots, with what I already know about each:

 · MACHINE POPS OUT (1->2) then a new one POPS IN (3->4). Two separate instances with a gap between
   them - the customise jack is torn down before the stage one is built. Needs one persistent unit
   across the handoff, or at minimum an overlap so neither is ever absent.
 · SUN POPS (2->3). Diagnosed: the sun is a DOM element in #synthwave-bg, and _voidSunSet hands an
   opaque gradient to scene.background in the same statement that demotes the DOM layer - the
   scene's own sky occludes the DOM sun instantly. The .sw-sun element already HAS a 1.2s opacity
   transition; it never gets to run. Fix is to sequence the fade ahead of the background handoff.
 · SHIP GROUND + LEVER POP IN (3->4), and the lever should simply exist the whole time - one
   instance, present during customisation too.
 · HE FLOATS about a foot above the lever on the way down.
 · FINAL FRAME: a large part of the machine sits across the camera. Needs the machine/portal moved
   back or the walk extended, without walking him far enough to fall in the hatch.

Co-Authored-By: Claude <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_018E59Wnrd86WzhTJXdwNo7H
