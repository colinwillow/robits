CMB1507 - the dragon had no way into a real match

You could not see it because there was no path for it to appear. _dragonTest() is a console call you
cannot make on a phone, and the test-range enemy pads - the other place I registered it - are
deliberately SKIPPED during a rumble. A creature that only exists behind a debug hook does not exist.

The manta arrived because of a hard-coded ambient spawner that knew about exactly one creature. That
is now a roster, so adding a creature to the world is a single entry:

  manta    50s, 760-1020 units out, glides in high
  dragon   62s, 430-690 units out, spawns low and bursts UP off the deck on its 'entrance' clip

The dragon spawns nearer than the manta on purpose: its arrival is a rise out of the ground, and an
entrance you cannot see is not an entrance.

ALSO WHY YOU WOULD HAVE MISSED IT ANYWAY
It inherits the manta's prey-relative cruise, and for this creature that settled at y=196 - more than
twice the manta's ~88, i.e. above the top of a phone screen. It could have arrived perfectly and
still never been in frame. There is now an altitude CEILING rather than a servo, so dives and swoops
still take it lower and it simply cannot drift up out of view.

  measured, no debug call, a normal match: manta and dragon both present, GLB loaded, cruise
  y=196 -> 97-106 (the manta sits at ~88), and its HP was already dropping - it is in the fight.

Knobs: _DRAGON_AMBIENT_T (when), _DRAGON_SPAWN_R (how far), _DRAGON_MAX_Y (how high).

Honest limitation: I still could not frame a clean photograph of it in the headless harness - four
attempts, and the camera work is costing more than it returns. Everything above is measured rather
than seen, so how it actually LOOKS flying around is your call on device.

Co-Authored-By: Claude <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_018E59Wnrd86WzhTJXdwNo7H
